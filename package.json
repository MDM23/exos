{
  "description": "Tests for the exos client runtime, and the script that vendors CLDR. Nothing here is needed to build or run an application; see the readme.",
  "license": "MIT OR Apache-2.0",
  "name": "exos-runtime-tests",
  "private": true,
  "scripts": {
    "cldr": "node crates/exos-cldr/generate.mjs",
    "test": "node --test crates/exos-cldr/*.test.js crates/exos/js/tests/*.test.js"
  },
  "type": "module",
  "devDependencies": {
    "cldr-core": "^48.2.0",
    "cldr-numbers-full": "^48.2.0",
    "jsdom": "^30.0.1"
  }
}
